import React from "react";
import AdminPagePlaceholder from "../AdminPagePlaceholder";

// General website settings: company info, logo/favicon, contact info,
// social links, SEO defaults, and other global configuration.
export default function SiteSettings() {
  return (
    <AdminPagePlaceholder
      title="Site Settings"
      description="Company info, logo & favicon, contact details, social links, and SEO defaults."
    />
  );
}
